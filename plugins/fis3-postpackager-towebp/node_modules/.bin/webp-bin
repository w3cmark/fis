#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webp-bin/bin/webp-bin" "$@"
  ret=$?
else 
  node  "$basedir/../webp-bin/bin/webp-bin" "$@"
  ret=$?
fi
exit $ret
